# 更新日志

本文件记录插件各版本的变更内容。版本号遵循 `主版本.次版本.修订号`，每次改动递增修订号。

格式参考 [Keep a Changelog](https://keepachangelog.com/zh-CN/)。

## [1.0.49] - 2026-08-27

### 修复
- MySQL 老库个人红包创建失败（`Unknown column 'rp_code'`）：原补列逻辑依赖 `information_schema` 探测，在部分部署下误判「列已存在」而跳过 ALTER。现改为直接对真实表执行 `SHOW COLUMNS` 探测，缺列立即补列并二次复核；兼容元组/字典两种游标行；补列失败或复核仍缺失时初始化显式报错终止（涉及资金，宁可失败也不静默降级）。

## [1.0.48] - 2026-08-22

### 新增
- 抢红包公布领取者：抢到红包的回复现在包含**抢的人昵称+ID**（昵称取群消息发送者，缺失时回退身份串）；昵称随领取记录一并落库。
- 抢完手气榜：红包被全部抢完时，最后一抢的回复附带**金额从多到少的领取排行榜**；超过 20 人只展示前 20，并注明「另有 N 人未展示」。
- 红包随机代码：红包对外标识改为 6 位随机码（小写字母+数字，剔除易混淆字符），`抢红包 <代码>` 即可；旧数字 ID 继续兼容直抢。数据库老库自动平滑补列（`rp_code` / `grabber_name`），无需手动迁移。
- 代码冷却复用：新红包生成代码时跳过「仍在有效期 + 过期后 7 天冷却期内」的代码；过期且冷却结束的代码允许复用。

## [1.0.47] - 2026-08-22

### 新增
- 管理员指令 `红包诊断`：在对话内直接查看「红包仅官机」运行状态——运行版本、开关、官机Markdown、你当前的身份判定（QQ=野机 / OpenID=官机），以及**最近 10 次红包指令的到达与判定记录**（时间、命令、触发者身份、拦截/放行）。
  - 若刚在群里发过红包指令却查不到记录，说明指令没有到达本插件实例——直接指向旧版本或重复副本的部署问题，无需再翻控制台日志；
  - 记录为内存环形缓冲（20 条封顶），重启即清零。

## [1.0.46] - 2026-08-22

### 优化
- 插件实例化时打印运行状态：`红包仅官机 official_only=… | 官机Markdown=… | 版本=…`，启动日志即可确认当前运行的代码版本与门控开关是否已加载，避免「改了配置但跑的是旧代码/旧副本」的排查盲区。

## [1.0.45] - 2026-08-22

### 优化
- 「红包仅官机」门控加固与可观测性：开关读取改为容错（嵌套读取失败回退关），并增加日志——开启调试模式（`debug_settings.enabled`）后，每次红包指令都会打印 `[红包仅官机][DEBUG] 开关=… 触发者=… type=… 野机判定=… → 拦截/放行`；实际拦截时另打印一行 INFO 说明被静默忽略的 sender。判定规则明确为「触发者身份为数字(QQ)=野机→拦截，非数字(OpenID)=官机→放行」。

## [1.0.44] - 2026-08-22

### 新增
- 官机 Markdown 回复：新增 `reply_settings.official_markdown`（默认开）。检测到请求来自官方机器人（OpenID 身份）时，回复文本自动按 QQ 原生 Markdown 发送（换行符转 `\r`）；野机始终保持纯文本。官方机器人无原生 Markdown 权限时，AstrBot 框架会自动回退普通文本。
- 全部 75 处回复出口收口到统一 `_reply` 通道，后续格式调整只需改一处。

## [1.0.43] - 2026-08-22

### 修复
- 「红包仅官机」开启时 `个人红包` 在野机不再可用：与其他红包指令一致静默忽略（此前漏挂门控）。
- `个人红包` 参数帮助文案误用管理员「发红包」的提示，现改为指向「个人红包」的独立文案（份数/额度缺失、格式错误四条）。

## [1.0.42] - 2026-08-22

### 新增
- 普通用户个人红包：新指令 `个人红包 [份数] [总额度]`，从发送者自己余额扣款（与管理员凭空发放的 `发红包` 区分）。
  - 额度门槛：每满 `user_send_balance_per_send`（默认 5）余额可发 1 次/日，余额 10 可发 2 次，以此类推；
  - 每日上限 `user_send_max_per_day`（默认 10）次，与余额门槛取较小值；当日计数随跨天自动重置，且随余额消耗实时收缩资格；
  - 红包总额不得超过自己当前余额；按网站账号加锁并在锁内二次验余，杜绝并发双花；建包失败自动全额退款。
- 身份验证：首次发个人红包前必须发送 `验证令牌 [网站访问令牌]`，插件调用 New API `/api/user/self` 校验令牌有效且与绑定网站 ID 对应，一次通过永久标记（野机 QQ / 官机 OpenID 用户通用）；提示令牌等同密码建议私聊验证。

## [1.0.41] - 2026-08-22

### 新增
- 报错文案统一附带反馈链接：所有插件内部失败类提示（绑定/解绑失败、签到异常、红包入账失败、数据库迁移失败等 13 类）自动追加 `🛠 遇到问题？请到 https://github.com/Tianxu-Astrbot-Plugins/astrbot_plugin_newapi_tx/issues 反馈`；用户输入校验类提示保持干净不带链接。
- 全局异常护栏：全部 14 个命令处理函数包裹异常捕获，未预期异常记录完整堆栈到后台日志，并向用户回复人类可读的错误摘要 + 反馈链接，不再把原始异常转储抛给框架。

## [1.0.40] - 2026-08-22

### 修复
- 官机(OpenID)使用「查询余额」报错 `'qq_id'`（KeyError）：余额缓存的更新逻辑直接取 `binding['qq_id']`，而 OpenID 绑定行没有该键；现回退为 `get('qq_id', get('openid'))`，与打劫/抢红包的取值方式一致。已全库排查其余 `['qq_id']` 直接访问点（消耗榜两处只查 QQ 绑定表、管理员查询仅处理 QQ 行），均不受影响。

## [1.0.39] - 2026-08-22

### 新增
- 「仅野机绑定改分组」开关：`binding_settings.wild_bind_group_only`（默认关）。开启后用户组晋升仅由野机(QQ号)绑定触发，OpenID(官机) 绑定不再改动网站用户组，绑定成功提示注明「官机绑定不改变用户组」。
- 「官机绑定需先绑QQ」开关：`binding_settings.openid_require_qq_bound`（默认关）。开启后网站 ID 必须已绑有 QQ 号（野机）才能在官机完成 OpenID 绑定，未绑时给出「请先在野机机器人处完成绑定」的引导提示且不落库。

## [1.0.38] - 2026-08-22

### 新增
- 「红包仅官机」开关：`red_packet_settings.official_only`（默认关）。开启后**所有红包指令仅可通过官方机器人（OpenID 身份）触发**，野机收到的请求（发红包、抢红包等）一律**静默忽略**——不回复、零消息量，避免刷屏触发野机风控；开启后红包公告自动追加「仅可通过官方机器人抢夺」提示。

## [1.0.37] - 2026-08-22

### 新增
- 双绑定共存明确化：同一网站 ID 可同时绑定一个 QQ 号（野机）与一个 OpenID（官机），两路身份均可签到、打劫、抢红包（底层双表本就支持，现补充可见性与文档）。
- 「野机优先」开关：`check_in_settings.wild_bot_priority`（默认开）。当签到同时经官机(OpenID)与野机(QQ号)触发时，官机侧自动让行约 1.5 秒再抢签到锁，保证野机先完成、官机侧得到「今日已签到」；仅官机单独触发时只多等这一固定延迟。
- 管理员「查询」命令现在会附带显示该网站账号的另一路绑定：存在 OpenID 绑定时追加一行 `🔗 另有 OpenID 绑定（官机）：{openid}`。

### 文档
- 重写 README：补全双绑定 / 野机优先、红包、消耗榜全员开放、数据库导入导出、调试模式等新增功能说明；指令表不再写死唤醒前缀；修正群白名单覆盖的指令清单（签到 / 查询余额 / 打劫 / 发红包 / 抢红包）；补充「勿重复安装 fork 副本」提示。同步修正消耗榜处理函数的过期 docstring。

## [1.0.36] - 2026-08-22

### 新增
- 红包功能（拼手气）：管理员 `发红包 数量 总额度` 凭空发放（不扣款），用户 `抢红包 红包编号` 参与抢夺，抢到的额度直接入账网站余额并实时展示「抢到多少 / 剩余多少份」。支持多红包并存、微信式随机拆分（总额精确守恒）；按红包 ID 加锁防并发超抢，领取记录唯一键防同一身份多次领取；24 小时过期（可配），份数/总额上限可配；参数非法时给出人类可读提示。

## [1.0.35] - 2026-08-22

### 新增
- 调试模式开关：`debug_settings.enabled`（默认关）。开启后输出消耗榜深度诊断日志——每行缓存原始值（repr 含类型）、最终取值、兜底查询命中/未命中、完整渲染全文与实例指纹，用于定位「绑定存在但 QQ 号不显示」类问题；日常使用关闭即可保持日志干净。

## [1.0.34] - 2026-08-22

### 变更
- 消耗榜增加渲染指纹日志（实例 ID + 渲染首行），配合排查可见回复是否出自当前代码。

## [1.0.33] - 2026-08-22

### 变更
- 消耗榜 QQ 号问题深度诊断：预填充记录实际查库次数与命中数、TOP3 user_id 类型；兜底回查显式记录命中/未命中等结果，用于定位「数据库有绑定但榜单不显示」的真实原因。

## [1.0.32] - 2026-08-22

### 修复
- 导入安全防护：迁移文件（transfer.db）四表全空时拒绝导入并明确提示，防止误用空文件清空当前数据库的绑定等数据。

## [1.0.31] - 2026-08-22

### 修复
- 修复消耗榜 QQ 号整榜不显示：QQ 展示此前完全依赖 KV 缓存一次性预填充成功，缓存被清空（如插件重载 `terminate()` 删缓存）或预填充批量查库闪失时整榜无 QQ 且无回退。现展示层对上榜用户逐条兜底回查数据库并回写缓存；预填充异常不再阻断出榜；新增 show_qq/缓存大小诊断日志便于定位。

## [1.0.30] - 2026-08-22

### 修复
- 修复导入/导出与 SQLite 模式在部分 AstrBot 版本报 `cannot import name 'get_astrbot_data_path'`：数据目录解析改为多版本兼容链（多个官方 API → StarTools → 由插件安装位置推导兜底），不再依赖单一导入路径。

## [1.0.29] - 2026-08-20

### 新增
- 数据库导入导出（管理员）：新增 `new-tx 导出` / `new-tx 导入` 指令，经独立迁移文件 `transfer.db` 在 MySQL 与 SQLite 间搬运数据。导出=把当前数据库的绑定、OpenID 绑定、签到状态、打劫日志四张表整体写入迁移文件；导入=清空当前库对应表后写入文件内容。典型用法：MySQL 模式下「导出」→ 切换为 SQLite 模式 → 「导入」即完成 MySQL→SQLite 迁移，反向同理。

## [1.0.28] - 2026-08-20

### 修复
- 提示文案不再写死 `/` 唤醒前缀：AstrBot 支持自定义唤醒前缀，所有运行时提示（未绑定引导、绑定用法/格式错误提示、打劫者未绑定默认模板等）改为只展示指令名（如「绑定 1234」），兼容任意前缀。

## [1.0.27] - 2026-08-20

### 修复
- `/绑定` 输入非数字网站ID（如 `/绑定 abc`）时不再抛框架类型转换异常，改为人类可读提示：缺失参数提示用法、格式错误回显输入并给出示例；`/解绑`、`/查询` 同步应用相同校验。

## [1.0.26] - 2026-08-20

### 新增
- OpenID 绑定支持：`binding_settings` 新增 `enable_openid_binding` 开关（默认关）。开启后，官方机器人（sender 为 OpenID 字符串）可直接 `/绑定 [网站ID]`，绑定关系独立存储于新表 `newapi_openid_bindings`（openid ↔ website_user_id），并自动晋升用户组；解绑/退群净化会同步清理 OpenID 绑定。
- 打劫支持文本目标：`/打劫` 除 @ 提及外，现支持直接输入 QQ 号（数字）或 OpenID（字符串，需开启 `enable_openid_binding`）作为目标。
- 消耗榜开放给所有用户：移除 `/消耗榜` 的 ADMIN 权限限制。

### 修复
- 修复打劫报 `'<' not supported between instances of 'str' and 'int'`：打劫并发锁的排序键统一转为 str，避免 OpenID(str) 与网站ID(int) 混合排序崩溃；打劫者身份改用 `get_user_by_identity` 同时兼容 QQ 号与 OpenID，日志键对 OpenID 用户取 website_user_id 以兼容 bigint 列。
- `require_binding` 装饰器改用 `get_user_by_identity`，同时支持 QQ 号与 OpenID 身份查找。

## [1.0.25] - 2026-08-20

### 修复
- 修复并发签到绕过「今日已签到」重复领奖漏洞：同一账号同时发多条 `/签到` 时，各请求在「读签到状态」与「写签到时间」之间交错执行，均读到未签到而各自加额度。现按 `website_user_id` 加 `asyncio.Lock` 串行化整段「读状态→加额度→写时间」，第二个并发请求等锁释放后读到已签到即直接返回，彻底杜绝 TOCTOU 重复领奖。
- 修复并发打劫突破每日次数/冷却/防御上限漏洞：多个 `/打劫` 并发时各自读到次数未达上限而全部放行，可突破 `max_attempts_per_day`、冷却与受害者 `max_defenses_per_day`。现按 `robber_qq_id` 与 `victim_website_id` 升序去重加锁，锁内重做次数/冷却/防御校验与资金划转；升序加锁避免交叉打劫死锁，`try/finally` 保证异常释放。

## [1.0.24] - 2026-08-20

### 修复
- 修复 `/调整余额 @对方 100` 金额被忽略、报「请提供金额」：AstrBot 命令解析会把 @ 段变成 `@昵称(QQ)` 文本占据参数位，真正金额在 `display_adjustment` 中；现统一从 `display_adjustment` 取金额（0 视为未提供），`@` 与非 `@` 场景均正确。
- KV 绑定缓存读-改-写竞态：并发绑定/解绑/消耗榜批量缓存可能互相覆盖丢失更新；新增 `_kv_lock`（asyncio.Lock）与 `_update_binding_cache()` 辅助方法，所有缓存更新与批量填充统一加锁。
- 打劫日志写入异常安全：`log_heist_attempt` 仅依赖数据库，若写入失败会抛异常让用户误以为操作失败（资金实际已划转）；现对成功/失败两个路径的日志调用加 try/except 兜底，日志丢失不再影响打劫结果返回。
- 移除无用的 `_parse_float_safe` 死代码。

## [1.0.23] - 2026-08-20

### 修复
- 根治 `/消耗榜` 在群聊中 QQ 号不显示的偶发电源：绑定关系改为存储到 AstrBot KV（`binding_cache` dict），展示完全走 KV 而非数据库，彻底规避瞬时 DB 连接故障；`execute_query` 加异常捕获与自动重试兜底。
- 绑定/解绑/退群净化时自动同步更新 KV 缓存；插件重载时 `terminate()` 生命周期钩子自动清空 KV 缓存。

## [1.0.22] - 2026-08-19

### 新增
- `/消耗榜` 新增 `show_qq` 开关（默认开）：控制是否在每行显示绑定用户的 QQ 号，与 `show_quota` 并列，可在 `consumption_leaderboard_settings` 中配置。

### 修复
- 修复打劫失败时赔偿额超过抢劫者余额会导致负数余额、凭空多钱的漏洞：`/打劫` 失败赔偿改为 `allow_partial=True`，扣款钳制在抢劫者可用余额内，永不产生负数，已扣款与失败次数正常记录。

## [1.0.21] - 2026-08-19

### 新增
- 群白名单（只监听配置群）：新增 `group_whitelist_settings` 配置，开启后 `/签到`、`/打劫`、`/查询余额` 仅在下方的 `group_list` 群号列表内响应，其余群聊与私聊一律静默忽略；其余指令（绑定、管理员指令、退群净化等）不受影响。

## [1.0.20] - 2026-08-19

### 新增
- `/消耗榜`（管理员）：展示全站用户近 24 小时 token 消耗排行榜，通过 New API 后台接口 `/api/log/` 聚合 `prompt_tokens + completion_tokens` 按用户排序；每行展示网站用户名、token 消耗量，若该网站用户已绑定则附带 QQ 号。可在 `consumption_leaderboard_settings` 中开启 `show_quota` 开关（默认关）额外显示网站额度消耗量，时间窗口与名次数量亦可配置。
- ⚠️ 依赖 New API 的「记录消耗日志」（LogConsumeEnabled）：若站点未开启消耗日志记录，榜单将为空。

## [1.0.19] - 2026-08-19

### 新增
- `/查余额`、`/调整余额` 支持 @ 提及目标：可 `@对方` 代替手输网站ID/QQ号；`/调整余额 @对方 100` 同样可用。
- 插件支持中英双语（i18n）：运行时回复文案按 `i18n_settings.language`（zh/en）切换；WebUI 文案通过 `.astrbot-plugin/i18n/en-US.json` 提供英文翻译。

## [1.0.18] - 2026-08-19

### 修复
- 补上多 QQ 轮流绑定同一网站账号刷奖励的漏洞：签到状态表 `newapi_check_in_state` 的去重键由 QQ 号改为**网站用户 ID**（额度最终落在网站账号上，故按账号去重），彻底堵住「多个 QQ 换绑同一个网站用户各领一次新人礼包与每日签到」；同时自动检测并重建 1.0.17 遗留的按 QQ 建的表。

## [1.0.17] - 2026-08-19

### 修复
- 修复「频繁退群进群刷新人礼包与签到奖励」漏洞：签到时间（含首次签到判定）原先存储在绑定记录上，退群/解绑净化会删除绑定记录，导致签到历史被清零、可重复领取。现新增独立持久化表 `newapi_check_in_state`（按 QQ 号唯一），退群/解绑不再清除；签到逻辑改为读写该表，旧数据在首次签到时自动迁移。

## [1.0.16] - 2026-08-19

### 修复
- 解绑/退群净化时用户组恢复失败：`revert_user_group` 原先将 GET 返回的完整用户对象（含 quota/password 等）原样 PUT 回 `/api/user/`，会被 New API 以 Invalid parameters 拒绝，导致用户组静默恢复失败。现与绑定仪式一致，只发送允许修改的字段（id/username/display_name/role/status/group）。
- 解绑后重新绑定报 "API group update failed"：绑定仪式在用户当前组已等于目标组时不再发送无意义的 no-op PUT，直接跳过用户组更新。

## [1.0.15] - 2026-08-18

### 新增
- 单 SQLite 模式：于独立 `sqlite_settings` 板块中开启 `use_sqlite_mode` 开关后，绑定与打劫日志存储于 `{AstrBot数据目录}/plugin_data/astrbot_plugin_newapi_tx/newapi.db` 单文件数据库（WAL 模式，自动建表），不再依赖 MySQL；`database_settings` 与 `.env` 中的 MySQL 配置将被忽略。⚠️ SQLite 与 MySQL 为两套独立数据，切换模式不会自动迁移，需重新绑定。
- `/pingapi` 响应新增「数据库引擎」行，显示当前使用 SQLite 还是 MySQL。

## [1.0.14] - 2026-08-17

### 变更
- 排行榜改用余额缓存：用户每次签到/查余额/打劫成功后顺手缓存余额，排行榜直接读缓存排序，完全无需查 API；移除旧策略的并发限制、查询数量上限、定时重建等配置。

## [1.0.13] - 2026-08-17

### 变更
- 排行榜适配千户规模：余额榜默认只查询最近绑定的前 200 个用户（`balance_query_limit` 可配），避免上千用户触发上千次 API 请求；打劫榜不受影响（纯 SQL 聚合）。

## [1.0.12] - 2026-08-17

### 变更
- 排行榜优化：余额查询并发限制为可配置（默认20），排行榜结果缓存默认一天（24小时）更新一次，减少 API 负载。

## [1.0.11] - 2026-08-17

### 新增
- 排行榜功能：`/榜单` 指令展示群余额榜（实时查询 API）与打劫榜（聚合打劫日志），可通过 `leaderboard_settings.enabled` / `top_n` 配置。

## [1.0.10] - 2026-08-17

### 新增
- 用户黑名单：在网站ID黑名单下方新增 `forbidden_user_ids`（禁止绑定的用户QQ列表），仅拦截新增绑定，已绑定不受影响。

## [1.0.9] - 2026-08-17

### 新增
- 绑定黑名单：管理员可在 WebUI 配置 `forbidden_website_ids`（禁止绑定的网站ID列表），仅拦截新增绑定，已绑定不受影响。

## [1.0.8] - 2026-08-17

### 新增
- 签到余额衰减功能：余额超过阈值后，签到随机额度最大值每超一档减半，最低值保持不变（可在 WebUI 通过 `diminish_enabled` / `diminish_threshold` 配置）。

## [1.0.7] - 2026-08-17

### 变更
- 整体重写 `README.md`：突出本插件为独立维护版本（非第三方 Fork），与旧版 Fork 文档区分；统一插件目录名 `astrbot_plugin_newapi_tx`，补充指令大全、版本与更新日志说明。

## [1.0.6] - 2026-08-17

### 新增
- 新增 `CHANGELOG.md` 更新日志机制，后续每次改动将在此追加记录并同步递增版本号。
